algoritmo "procedimentoSoma"
var
    x,y:  inteiro
    procedimento soma(a,b:inteiro)
    inicio
    escreval("recebi o valor: ",a)
    escreval("recebi o valor: ",b)
    escreval("a soma do valores e ",a+b)
    fimprocedimento
inicio
      x:=5
      y<-3
      soma(x,y)
fimalgoritmo
